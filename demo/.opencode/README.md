# Generated Tool Adapters

This directory shows the optional OpenCode adapter surface generated by `chitking init`.

Adapters can help agents load Chitking role context, but they are not the durable research truth. In this committed demo, runtime product state is intentionally not checked in; run `chitking init` locally to create `.chitking/`. The user-owned research source remains in `research/`.
