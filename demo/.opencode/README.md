# Generated Tool Adapters

This directory shows the optional OpenCode adapter surface generated by `chitking init`.

Adapters can help agents load Chitking role context, but they are not the durable research truth. The durable product state remains in `.chitking/`, and the user-owned research source remains in `research/`.
