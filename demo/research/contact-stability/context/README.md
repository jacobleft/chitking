# Generated Context Packets

This directory is the cache/context boundary for the `contact-stability` thread.

Run `chitking dispatch [--role <role>]` to generate packet YAML here. Those YAML packets point back to `research/project.md` and `research/contact-stability/thread.md`; they are not durable product truth and are ignored by `demo/.gitignore`.
